# Millo 3 Environment Variables

# Stripe API Keys
# Get your keys from: https://dashboard.stripe.com/apikeys

# Stripe Publishable Key (used in checkout.js on client-side)
# Publishable key is already configured in js/checkout.js
# This is safe to be public and is already in the codebase

# Stripe Secret Key (used in server.js on server-side)
# Set this environment variable before starting the server:
# Get your actual secret key from Stripe Dashboard or STRIPE_KEYS.md (local file, not committed)
STRIPE_SECRET_KEY=your_stripe_secret_key_here

# Example usage:
# export STRIPE_SECRET_KEY="your_secret_key_here"
# npm start
